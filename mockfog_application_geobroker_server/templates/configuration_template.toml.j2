# server information
[server]
brokerId = "{{ hostvars[inventory_hostname].testbed_config.name }}"
port = 5559
granularity = 5
messageProcessors = 2
logConfFile = "/files/log4j2.xml" # specify another config file to be used

    # server mode specific information
    [server.mode]
    name = "single"
